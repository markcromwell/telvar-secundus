extends Node


func fade_to(scene_path: String, spawn_id: String = "") -> void:
	# Stub for HTML5 / CI environments; full implementation lives in the main game tree.
	push_warning("SceneTransition.fade_to(%s, %s) — stub" % [scene_path, spawn_id])
